#include<stdio.h>
int main()
{
  int a,b;
  scanf("%d",&a);
  scanf("%d",&b);
  if((a*a)<(b*b))
    printf("circle can be inside a square");
  else
    printf("circle cannot be inside a Square");//Fill your code
}